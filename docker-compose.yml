version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgres_hello_world
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: tajnehaslo123
      POSTGRES_DB: postgres
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
